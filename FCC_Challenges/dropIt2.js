// Use 'filter()'

function dropElements(arr, func) {
  // Write here.
}


// Use 'shift();

// Use 'for loop' and 'br
eak'


console.log(
  dropElements([1, 2, 3, 4], function(n) {
    return n >= 3;
  })
);
